{
  "name": "profesource-backend",
  "version": "0.0.1",
  "description": "Backend for Profesource App",
  "main": "server.js",
  "scripts": {
    "start": "cross-env NODE_ENV=development node server.js",
    "server:dev": "cross-env NODE_ENV=development env-cmd -f .env nodemon server.js --config nodemon.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Edison Mora",
  "license": "ISC",
  "engines": {
    "node": ">=12.0.0"
  },
  "dependencies": {
    "bcrypt": "^5.0.0",
    "cors": "^2.8.5",
    "cross-env": "^7.0.2",
    "env-cmd": "^10.1.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "morgan": "^1.10.0",
    "winston": "^3.2.1",
    "winston-daily-rotate-file": "^4.5.0"
  },
  "devDependencies": {
    "eslint": "^7.2.0",
    "nodemon": "^2.0.4"
  }
}
